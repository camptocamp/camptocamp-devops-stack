== Deploy the cluster

```shell
$ terraform init
$ terraform apply
```

You should see the services URL as Terraform outputs:

* alertmanager_url
* argocd_url
* grafana_url
* keycloak_url
* prometheus_url
